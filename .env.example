# Copiar este archivo como .env.production o .env.staging según el entorno.
#
# VITE_BASE_PATH: ruta base donde se publicará el sitio.
# - Despliegue en la raíz del dominio (https://midominio.com/):      "/"
# - Despliegue en subcarpeta (https://midominio.com/evaluacion/):    "/evaluacion/"
VITE_BASE_PATH=/
